import 'package:flutter/material.dart';

class SettingsScreen extends StatelessWidget {
  const SettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Sozlamalar sahifasi',
        style: TextStyle(color: Color(0xFF0F2F2B), fontSize: 16),
      ),
    );
  }
}
